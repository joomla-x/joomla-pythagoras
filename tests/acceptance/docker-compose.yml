web:
  image: php:5-apache
  volumes:
    - ../..:/var/www/html
  ports:
    - 8080:80
  links:
    - db:db

db:
  image: mysql
  volumes:
    - ../../tests/_envs/sql:/var/lib/mysql
  ports:
    - 3306:3306

hub:
  image: selenium/hub
  ports:
    - 4444:4444

firefox:
  image: selenium/node-firefox-debug
  ports:
    - 5900
  links:
    - hub:hub

#chrome:
#  image: selenium/node-chrome-debug
#  ports:
#    - 5900
#  links:
#    - hub:hub
